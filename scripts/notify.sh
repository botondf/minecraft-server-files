#!/bin/bash

#yet to do:

#email on update
# - there may be a spigot version command you can access with rcon
# - optionally look for obvious ***'s in server console at startup (no jctl access so idk how)

#email on crash
# - when service fails or exits with some error code